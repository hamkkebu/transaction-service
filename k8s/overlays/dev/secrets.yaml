# Dev 환경: K8s Secrets 직접 사용
apiVersion: v1
kind: Secret
metadata:
  name: transaction-service-secrets
  labels:
    app: transaction-service
    environment: dev
type: Opaque
stringData:
  DB_USERNAME: "${DB_USERNAME}"
  DB_PASSWORD: "${DB_PASSWORD}"
