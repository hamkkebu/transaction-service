apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: transaction-service-secrets
  namespace: hamkkebu
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: hamkkebu-secret-store
    kind: SecretStore
  target:
    name: transaction-service-secrets
    creationPolicy: Owner
  data:
    - secretKey: DB_USERNAME
      remoteRef:
        key: transaction-service-db-credentials
        property: username
    - secretKey: DB_PASSWORD
      remoteRef:
        key: transaction-service-db-credentials
        property: password
---
# Source secret for dev environment (Kubernetes backend)
# In production, this would come from AWS Secrets Manager
apiVersion: v1
kind: Secret
metadata:
  name: transaction-service-db-credentials
  namespace: hamkkebu
  labels:
    app: transaction-service
type: Opaque
stringData:
  username: "admin"
  password: "CHANGE_ME_IN_CLUSTER"
