# ========================================
# Transaction Service 환경 변수 설정
# ========================================
#
# 사용 방법:
# 1. 이 파일을 .env로 복사: cp .env.example .env
# 2. 실제 환경에 맞게 값 수정
# 3. .env 파일은 Git에 커밋하지 마세요!

# ========================================
# 포트 설정 (Transaction Service 전용)
# ========================================
# Transaction Service: Backend 8083, Frontend 3003, gRPC 9094, MySQL 3309
# Auth Service: Backend 8081, Frontend 3001, gRPC 9091, MySQL 3307
# Boilerplate: Backend 8080, Frontend 3000, gRPC 9090, MySQL 3306

# Backend HTTP 포트
BACKEND_PORT=8083

# Frontend 개발 서버 포트 (docker-compose)
FRONTEND_PORT=3003

# gRPC 서버 포트
GRPC_SERVER_PORT=9094
GRPC_PORT=9094

# ========================================
# Database Configuration
# ========================================
DB_HOST=localhost
DB_PORT=3309
DB_NAME=hamkkebu_transaction
MYSQL_ROOT_PASSWORD=root

# ========================================
# Auth Service gRPC Connection
# ========================================
AUTH_SERVICE_HOST=hamkkebu-auth-service-backend
AUTH_SERVICE_GRPC_PORT=9091

# ========================================
# Redis Configuration (공유)
# ========================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=""

# ========================================
# Kafka Configuration (공유)
# ========================================
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# ========================================
# Application Configuration
# ========================================
SPRING_PROFILES_ACTIVE=dev
